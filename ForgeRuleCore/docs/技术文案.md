# ForgeRuleCore 代码审查技术文案

> **审查对象**：`ForgeRuleCore` Swift Package（rule kernel）  
> **审查日期**：2026-07-19  
> **范围**：Core / Matchers / Geo / GeoMMDB / Config / Tests  
> **结论摘要**：内核边界清晰、核心路径可测；当前处于 **narrow compiler** 阶段，存在若干正确性与生命周期风险，不宜直接按「完整 Xray/Surge 兼容层」对外承诺。

---

## 1. 项目定位

ForgeRuleCore 是 QuantumLink / NetForge 生态中的 **L3 策略规则内核**：

```
FlowFactsResolver  →  RuleEngine.evaluate  →  RuleAction / RouteDecision
        ↑                     ↑
   FakeIP / domain      GeoSiteDB + GeoIPDB
```

| 职责内 | 职责外 |
|--------|--------|
| geosite / geoip 加载与匹配 | DNS 应答、DoH、fallback 编排 |
| 域名 / Geo 条件评估 | 包转发、socket 生命周期 |
| `RuleInput` → Direct / Proxy / Reject | NE / UI / 规则订阅下载 |

定位正确：包内保持「不可变快照 + 纯评估」，热更新与编排留给上层。

---

## 2. 架构总览

```
libmaxminddb (C)
  └── GeoMMDBBridge (全局 g_db + refcount)
        └── ForgeRuleCore
              ├── Core        Rule / RuleEngine / Flow* / Config / Bundle
              ├── Geo         GeoSiteDB / GeoIPDB
              ├── Matchers    ExactSet / SuffixTrie / Keyword
              └── Adapters    CountryLookup
```

### 2.1 分层评价

| 层 | 评价 | 说明 |
|----|------|------|
| Kernel (`RuleEngine`) | 好 | 顺序扫描语义明确，Sendable 快照模型合适 |
| Flow Adapter | 好 | Resolver → Core 边界清楚，便于注入 FakeIP |
| Config Compiler | 偏弱 | narrow 语义可接受，但静默丢规则缺少诊断 |
| GeoSite | 中等 | full / suffix / keyword 索引合理；regex 静默忽略 |
| GeoMMDB | 风险高 | 全局 C 状态 + Swift reader 生命周期耦合 |

---

## 3. 稳定契约（审查认定）

以下行为视为当前 **事实契约**（与实现一致）：

1. `RuleEngine` 初始化后规则与 Geo DB 引用不可变；reload 需外层 snapshot swap。
2. `evaluate` 会规范化域名（小写、去首尾空白与 `.`）；空域名视为 `nil`。
3. 规则按数组顺序扫描；**首个非 `.any` 命中立即返回**。
4. `.any` 为 fallback：更新默认动作后继续扫描，允许后续具体规则覆盖。
5. 无命中时默认 `.direct`。
6. 域名类条件在 `domain == nil` 时不命中。
7. `.geoip` 使用 `resolvedIP`，不是 `originalIP`。
8. `FieldRoutingRuleFactory`：**每个 field row 最多一条 primitive 条件**（单个 domain 或单个 `geoip:`）。

### 3.1 条件支持矩阵

| 条件 | 状态 | 备注 |
|------|------|------|
| DOMAIN / `full:` | ✅ | `domainFull` |
| DOMAIN-SUFFIX / `domain:` | ✅ | `domainSuffix`；无前缀按 suffix |
| DOMAIN-KEYWORD / `keyword:` | ✅ | 线性 `contains` |
| GEOSITE | ⚠️ 子集 | `full`/`domain`/`plain`；`regex` 忽略 |
| GEOIP / `geoip:!cc` | ⚠️ 子集 | 国家码；非 CIDR 列表 |
| FINAL | ⚠️ 近似 | 用 `.any` 表达，语义非 Surge 单条垫底 |
| IP-CIDR / IP-CIDR6 | ❌ | 未实现 |
| network / port / AND / OR | ❌ | factory 拒绝或未接线 |

---

## 4. 代码质量审查发现

按严重级别排序。

### P0 — 正确性 / 生命周期

#### F1. MMDB 全局单例与多路径冲突

`GeoMMDBBridge.c` 使用进程级 `g_db` + `g_refcount`：

- 第二次 `forge_mmdb_open(不同路径)` 在 `prev > 0` 时直接 `return 0`，**不会切换数据库**。
- `MMDBReader.reopen()` 会 `close` 再 `open(self.path)`，可能关掉其它 reader 正在用的库。
- `forge_mmdb_country_ipv4` 在查找期间临时 `refcount++`，与 Swift 侧 `DispatchQueue` 串行保护叠加，语义复杂，难推理。

**影响**：多实例、热更新、测试并行打开不同 mmdb 时行为未定义。  
**建议**：改为「每实例持有 `MMDB_s*`」或明确文档约束「进程内仅允许一个活跃 MMDB」，并去掉误导性的 `reopen()` 公开 API，直到有真正的 snapshot provider。

#### F2. Config compiler 静默丢规则

`FieldRoutingRuleFactory.makeRules` 使用 `compactMap`：

- 多 domain、domain+ip、可识别 network、`regexp:`、非法 tag → 全部变成「规则消失」。
- 上层无法区分「用户没写」与「写了但被丢弃」。

**影响**：线上分流行为与配置意图静默偏离，极难排查。  
**建议**：引入 `CompileResult` / diagnostics（accepted / rejected + reason），至少在 DEBUG / 启动日志中汇总拒绝条数。

#### F3. `geoip:!cc` 在 lookup 失败时恒为 true

```swift
return !matchPositive(key: inner, ip: ip)
// matchPositive 在 lookup 返回 nil 时为 false
// 因此 !false == true
```

未知国家 / 私网 / MMDB miss 的 IP 会命中所有 `geoip:!cn` 类规则。  
这可能是期望行为（「非 CN 走代理」），但必须在契约中写死，并补测试；否则集成方会按「明确非 cn」理解而踩坑。

---

### P1 — API / 语义一致性

#### F4. Public API 面过宽

几乎所有 matcher、JSON model、`MMDBReader`、`GeoIPProvider` 均为 `public`。  
稳定内核实际只需要：

- `Rule` / `RuleInput` / `RuleAction` / `RuleEngine` / `RuleCore`
- `FlowRuleClassifier` / `FlowFactsResolving` / `FakeIPStore`
- Config DTO + Factory
- `CountryLookup`（注入点）
- Bundle 装配入口

**风险**：内部结构（trie 节点布局、MMDB bridge）成为外部依赖后，重构成本高。  
**建议**：下一版本标注「stable vs internal」，或逐步改为 `package` / 非 public。

#### F5. `.any` 与 Surge `FINAL` 语义不一致

当前 `.any` 可放在列表前部作为「可被覆盖的默认动作」。  
Surge `FINAL` 习惯是「最后一条，之后不再匹配」。  
两者都能表达默认路由，但配置迁移时容易写错。

**建议**：文档固定推荐写法（`.any` 放末尾，且其后不再放规则），或新增显式 `final` 条件并在引擎中短路。

#### F6. GeoSite `regex` 与 attributes 被忽略

- `regex` 类型 DEBUG 打印后 `continue`。
- `attributes` 解码但不使用。

若官方 geosite 依赖 regex / `@cn` 等 attribute，行为会与上游数据静默不一致。

---

### P1 — 实现细节

#### F7. `DomainSuffixTrie` 查询路径额外分配

`containsSuffix` 对每个 label 执行 `String(labelSub)`。  
GeoSite 热路径上，标签数虽小，但在 Extension 高频 classify 中仍可优化为 `Substring` 字典键或 UTF8 视图。

另：局部变量名 `tire` 疑似 `trie` 笔误，不影响正确性，影响可读性。

#### F8. `DomainKeywordMatcher` 线性扫描

复杂度 \(O(K \cdot |d|)\)。当前 geosite keyword 规模下可接受；若未来合并大量 plain 规则，应考虑 Aho-Corasick。

#### F9. `GeoIPDB` 缓存键与双重 Optional

`parsedKeyCache: [String: CountryCode?]` + `if let cached = parsedKeyCache[norm]` 依赖 Swift 对「字典值本身是 Optional」的解包语义，可读性差，易在后续改动中引入「缓存 miss / 命中 nil」混淆。

Preheat 用 `if cache[positive] == nil` 无法区分「未插入」与「插入了失败解析」；功能上大多无害，但应改成显式 `contains` 或非 Optional 值 + tombstone。

#### F10. `RuleEngine` 与 Resolver 重复 normalize

`FlowFactsResolver` 与 `RuleEngine.evaluate` 都会 `normalizeDomain`。  
防御性重复可接受，但应在契约中声明「evaluate 始终自规范化」，避免调用方以为必须先 resolve。

#### F11. `RuleInput.port` / `proto` 未被引擎消费

字段已进入输入模型，但 `matches` 完全忽略。  
与 config 侧 `parseNetworkList`「解析后拒绝」一致地表明：**组合条件尚未接线**。  
建议在类型或文档上标记 `reserved`，避免集成方误以为已支持端口分流。

---

### P2 — 测试与工程

#### F12. 测试覆盖偏单元、缺集成

现有约 31 个 `@Test`，对 narrow contract 覆盖良好：

- FlowFactsResolver
- FieldRoutingFactory / JSON decode
- Matchers / GeoIP stub / GeoSite temp JSON
- RuleEngine 顺序与 fallback

缺口：

- 真实 / 小型 `geoip.mmdb` fixture
- 官方 geosite / routing 样例兼容
- compiler 拒绝原因断言（待 diagnostics API）
- 并发 classify + reload
- `ForgeRuleCoreBundle` App Group 路径（需 mock FileManager 或注入 URL）
- `geoip:!cc` + lookup miss 契约测试

#### F13. Bundle 装配硬编码文件名

`geosite.json` / `geoip.mmdb` 写死在 App Group 根目录；无版本号、无校验、无「文件缺失」细分错误（仅 App Group URL 无效）。  
对 Extension 启动排障不够友好。

---

## 5. 复杂度速查（评估热点）

| 组件 | 时间 | 空间 |
|------|------|------|
| `RuleEngine.evaluate` | \(O(R \cdot C)\) | \(O(1)\) 额外 |
| `DomainSuffixTrie` 查询 | \(O(L)\) 标签数 | 与数据线性 |
| `DomainExactSet` | \(O(|d|)\) 均摊 | \(O(n)\) |
| `DomainKeywordMatcher` | \(O(K \cdot |d|)\) | \(O(K)\) |
| `GeoSiteDB.contains` | dict \(O(1)\) + 上三者 | 全量索引 |
| `GeoIPDB.match` | 解析 + MMDB lookup | preheat 缓存 |

当前瓶颈更可能在 **MMDB 串行队列 + 全局锁**，而非域名 trie。在补 CIDR / 组合条件前，不建议过早做规则索引。

---

## 6. 总体评级

| 维度 | 分数（1–5） | 说明 |
|------|-------------|------|
| 架构清晰度 | 4 | 内核 / adapter / geo 分层清楚 |
| 契约诚实度 | 3 | 代码注释与 ARCHITECTURE 已收敛；历史 README 曾过度承诺 |
| 正确性 | 3 | 核心匹配逻辑稳；MMDB 生命周期与静默丢规则拉低 |
| 可测试性 | 4 | 协议注入友好；缺真实 fixture |
| 可演进性 | 3 | public 面过宽；缺 composite condition 模型 |
| 生产就绪（VPN Extension） | 2.5 | 需先解决 MMDB 单例约束与 compiler 诊断 |

**综合**：适合作为 **MVP rule kernel** 继续迭代；不适合在未修 P0 前宣称「生产级完整分流引擎」。

---

## 7. 建议落地顺序（与架构 TODO 对齐）

### 立刻（P0）

1. 文档 / 契约写死：`geoip:!cc` 在 miss 时的行为；MMDB「单实例」或改造为 per-instance。
2. `FieldRoutingRuleFactory` 增加 diagnostics，禁止静默丢规则无痕迹。
3. 为 F1 / F3 补回归测试。

### 下一迭代（P1）

1. `IP-CIDR` / `IP-CIDR6` matcher + factory 接线。
2. Composite model：domain OR、domain+ip+network AND。
3. 收窄 public API；明确 `.any` / FINAL 推荐写法。

### 集成期（P2）

1. `RuleCoreSnapshot` atomic swap（真正的 reload）。
2. Bundle 错误细分与文件版本。
3. NetForge `PolicyDecision` 映射 + QuantumLink 热更新消息。

### 优化期（P3）

1. Trie / Keyword 分配与扫描优化。
2. 官方 fixture 兼容套件。
3. 评估 geosite regex / attributes。

---

## 8. 审查方法说明

- 静态阅读：`Core`、`Matchers`、`Geo`、`GeoMMDB`、`Package.swift`、既有测试与 `ARCHITECTURE.md`。
- 对照契约：实现 vs README / 架构文档 / `Rule.swift` 注释。
- 未在本次审查中重新执行 `swift test`；既有测试集在此前会话中曾全部通过（31）。
- 未对 `libmaxminddb` 上游 C 代码做安全审计；关注点在 Forge 封装层。

---

## 9. 相关文档

- [ARCHITECTURE.md](./ARCHITECTURE.md) — **权威架构与契约**（已吸收本文 F1–F3、reserved 字段、API 边界与 TODO 重排）  
- [QuantumLink 技术升级.md](../../../QuantumLink/docs/技术升级.md) — 跨仓本地工程 Review 与升级路线  
- [README.md](../../README.md) — 包入口与 narrow config 说明  

### 文案 → 架构文档吸收对照

| 审查项 | 架构文档落点 |
|--------|----------------|
| F1 MMDB 全局单例 | §5.1 硬约束；P0 TODO |
| F2 静默丢规则 | §4.5 + P0 diagnostics |
| F3 `geoip:!` miss → true | §4.3 契约写死 |
| F4 public 过宽 | §4.1 stable / 非 stable |
| F5 `.any` vs FINAL | §4.2 推荐置底 |
| F11 port/proto 未消费 | §4.1 reserved 表 |
| P0/P2 优先级不一致 | §9 以审查为准重排 |

---

*本文档为代码审查产出（过程记录）；日常集成与演进以 ARCHITECTURE.md 为准。*
